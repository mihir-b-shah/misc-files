
#ifndef FIXED_VECTOR
#define FIXED_VECTOR

#include "linear_algs.h"

float dot(const int N, const float* v1, const float* v2);
float add(const int N, float* v1, const float* v2, const float scl);

#endif